# ----------------- Python -----------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.python-version

# ----------------- Web app: secrets & runtime data ---------
# NEVER commit these — they contain student PII or plaintext passwords.
stempel-wanderung/data/
stempel-wanderung/credentials.csv
stempel-wanderung/login_cards.pdf
.env

# ----------------- Class roster (student PII) -------------
Auswahl_Person*.csv
roster*.csv

# ----------------- Student submissions (PII) --------------
# Raw student notebooks, reports, photos and grading notes. NEVER commit.
# The gallery (stempel-wanderung/static/gallery/) is the only rendered,
# anonymised output that ships; everything below stays local.
submissions/
bewertung/
# private Projekt-NN -> name map for the teacher
gallery_mapping.csv

# ----------------- Stale nested export --------------------
# Old one-off snapshot with its own .git; not part of the deploy.
pixel/

# ----------------- LaTeX build artifacts ------------------
# We keep skript.tex and skript.pdf in the repo. Everything else
# xelatex/biblatex generates is transient.
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.bbl
*.blg
*.run.xml
*.synctex.gz
*.fdb_latexmk
*.fls
*-blx.bib
skript_tmp.*

# ----------------- OS / Editor ----------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
